<%= form_with(model: pet_history, local: true) do |form| %>
  <% if pet_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet_history.errors.count, "error") %> prohibited this pet_history from being saved:</h2>

      <ul>
      <% pet_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container my-4">
    <div class="field">
      <%= form.label :pet %>
      <%= form.select :pet_id, options_for_select(@pets), prompt: "Select pet" %>
    </div>
    <div class="row">
      <div class="form-group col-4">
        <%= form.label :weight %>
        <%= form.text_field :weight %>
      </div>

      <div class="form-group col-4">
        <%= form.label :heigth %>
        <%= form.text_field :heigth %>
      </div>
    </div>
      <div class="form-group">
        <%= form.label :description %>
        <%= form.text_area :description %>
      </div>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
